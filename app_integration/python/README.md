# Python MySQL Integration Example

This example demonstrates how to connect to the `school_management` MySQL database using Python.

## Setup

1. Make sure your MySQL database is running (you can use the docker-compose from the root directory).
2. Install the required dependencies:
   ```bash
   pip install -r requirements.txt
   ```
3. Run the script:
   ```bash
   python main.py
   ```
